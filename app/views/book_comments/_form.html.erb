<%= form_with model: [book,book_comment], local: false do |f| %>
   <%= f.text_area :comment, rows: '5',cols:'80',placeholder: "コメントをここに",id: "comment_textarea" %>
   <br><%= f.submit "送信" %>
 <% end %>